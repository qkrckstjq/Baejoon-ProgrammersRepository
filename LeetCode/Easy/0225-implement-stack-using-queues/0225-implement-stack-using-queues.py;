from collections import deque


class MyStack(object):

    def __init__(self):
        self.stack = deque()

    def push(self, x):
        self.stack.append(x)

    def pop(self):
        if len(self.stack) != 0:
            return self.stack.pop()
        else:
            return None
    def top(self):
        if len(self.stack) != 0:
            # 큐의 마지막 원소는 직접 인덱스로 접근할 수 없음
            return self.stack[len(self.stack)-1]
        else:
            return None  # 큐가 비어있는 경우 None 반환

    def empty(self):
        return len(self.stack) == 0