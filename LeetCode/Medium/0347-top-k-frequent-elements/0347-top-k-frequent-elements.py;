from collections import Counter

class Solution(object):
    def topKFrequent(self, nums, k):
        count = Counter(nums)
        most_common = count.most_common(k)
        top_k_frequent = [item[0] for item in most_common]
        
        return top_k_frequent